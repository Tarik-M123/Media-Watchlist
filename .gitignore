# Secrets — never commit. Use .env.example as the template.
.env
.env.local
.env.*.local

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Node / frontend
node_modules/
dist/
dist-ssr/
*.local

# Playwright MCP scratch output — page snapshots and console logs written
# during browser-driven QA runs. Transient debris, not project files.
.playwright-mcp/

# Editors / tooling
# Local Claude Code config — contains machine-specific paths and permissions.
.claude/
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
